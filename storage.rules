rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read for all files (needed for marketplace images)
      // and allow write for authenticated users.
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
